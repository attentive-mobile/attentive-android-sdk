<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application>
        <activity android:name=".push.AttentivePush$PermissionRequestActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            />
        <service
            android:name="com.attentive.androidsdk.AttentiveSettingsService"
            android:exported="true"
            tools:ignore="ExportedService" />

        <!--
            Negative priority ensures a host app's own FirebaseMessagingService (default
            priority 0) wins FCM dispatch when declared. FCM uses PackageManager.resolveService,
            which honors android:priority. See MSDK-343 for context.
        -->
        <service
            android:name="com.attentive.androidsdk.push.AttentiveFirebaseMessagingService"
            android:exported="false">
            <intent-filter android:priority="-500">
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>


</manifest>